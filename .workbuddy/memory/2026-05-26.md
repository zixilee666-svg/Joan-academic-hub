# 2026-05-26 工作日志

## EdgeOne Pages 部署

- **时间**: 00:22 GMT+8
- **项目**: project_v6-main (学术管理平台 academic-hub-v4)
- **部署目标**: EdgeOne Pages
- **部署结果**: ✅ 成功
- **部署 URL**: https://project-v6-main-xuevfy89.edgeone.cool?eo_token=070f68f4a9ac3cf846cbd3180faf0312&eo_time=1779726256
- **项目 ID**: pages-itdzdcawkndt
- **部署 ID**: pkl7kif14p
- **控制台 URL**: https://console.cloud.tencent.com/edgeone/pages/project/pages-itdzdcawkndt/deployment/pkl7kif14p
- **部署区域**: 全球区域 (global area)
- **账号**: 猫惑 (Account ID: 100047679379)

### 备注
- CLI 版本: 1.4.5
- 构建时间: 2.38s
- 构建输出目录: dist/
- 配置文件: .edgeone/project.json (新版本 CLI 使用 project.json 而非 edgeone.json)

## Edge Functions 路由修复 (00:30-00:40)

- **问题**: 部署后 API 返回 XML 错误 `Unexpected token '<', "<?xml vers"... is not valid JSON`
- **根因1**: edge-functions/index.js 仅匹配根路径 `/`，`/api/*` 请求未路由到 Edge Function
- **根因2**: KV 访问方式错误——EdgeOne Pages KV 是全局变量，非 `context.env` 属性
- **修复1**: 移动 `edge-functions/index.js` → `edge-functions/api/[[default]].js`（匹配 `/api/*` 路由）
- **修复2**: 更新 KV 初始化，从 `env.ACADEMIC_HUB_KV` 改为 `globalThis.ACADEMIC_HUB_KV` 全局变量
- **验证**: GET `/api/spaces` 返回正常 JSON；POST `/api/auth/login` 返回 JSON UNAUTHORIZED（因 KV 未绑定）
- **新部署 URL**: https://project-v6-main-xuevfy89.edgeone.cool?eo_token=03e40a9c35aac47f42c6bd0401c7bbda&eo_time=1779727236
- **新部署 ID**: zx2bgnut6j
- ⚠️ 仍需在控制台绑定 KV Storage 命名空间才能使认证和数据持久化功能正常

## KV 绑定完成 + 重新部署 (00:55)

- **KV 命名空间**: `academic_hub_kv` → 已绑定到 Edge Functions，变量名 `ACADEMIC_HUB_KV`
- **重新部署 ID**: 091kck9jz4
- **重新部署 URL**: https://project-v6-main-xuevfy89.edgeone.cool?eo_token=ba7bbe100494dba32181fe38d886dbd8&eo_time=1779728432
- **控制台 URL**: https://console.cloud.tencent.com/edgeone/pages/project/pages-itdzdcawkndt/deployment/091kck9jz4
- **验证结果**:
  - ✅ `GET /api/spaces` → 返回 admin 空间数据（KV 读取正常）
  - ✅ `POST /api/auth/login` → 登录成功，返回 JWT token（KV 读写正常）
  - ✅ 前端页面正常加载
- **只涉及 1 个 KV 命名空间**: `ACADEMIC_HUB_KV`（承载 users、papers、projects、libraries、materials、spaces、system 全部数据）

## Admin Stats 崩溃修复 (01:04-01:11)

- **问题**: 管理后台读取数据失败，`/api/admin/stats` 返回 545 错误（Edge Function 崩溃）
- **根因1**: 第 2404 行 `for (let i = 0; i < 86400; i++)` 逐秒 KV 查询导致超时崩溃（86,400 次 KV get）
- **根因2**: `ACADEMIC_HUB_KV.list()` 未经 try-catch 保护，API 不兼容时直接崩溃
- **根因3**: KV list 返回的 key 对象格式不兼容（非 Cloudflare 标准 `{name}` 格式）
- **根因4**: 每日指标计数器 `system:metrics:daily:*` 只读不写，导致数据始终为 0
- **修复1**: 用每日计数器替代逐秒 KV 循环（2 次 KV 读 vs 86,400 次）
- **修复2**: `ACADEMIC_HUB_KV.list()` 加 try-catch 保护
- **修复3**: KV key 对象兼容多种格式（`k.name` / `k.key` / `k.id` / string）
- **修复4**: `recordRequestMetrics` 中新增每日计数器写入
- **修复5**: 整体 admin/stats handler 加 try-catch + apiError 兜底
- **最终部署 ID**: ngehhtza86
- **最终部署 URL**: https://project-v6-main-xuevfy89.edgeone.cool?eo_token=3cc6c74da74db73918b15b1f1bfb7374&eo_time=1779729420
- **验证**: `/api/admin/stats` 正常返回 → totalUsers:1, totalPapers:5, kvUsage:11

## GitHub 仓库创建 (09:55)

- **仓库名**: Joan-academic-hub
- **GitHub URL**: https://github.com/zixilee666-svg/Joan-academic-hub
- **可见性**: Public
- **分支**: main
- **初始提交**: f3b27be - 226 files, 45,045 lines
- **描述**: Joan Academic Hub v4 - AI-driven academic paper management platform with React 19 + TypeScript + EdgeOne Pages

## EdgeOne Pages 自动部署 (09:58)

- **部署来源**: GitHub 仓库 Joan-academic-hub
- **部署 ID**: s23vf8trec
- **部署 URL**: https://project-v6-main-xuevfy89.edgeone.cool?eo_token=bb85892644f8417c45c019fa6e102158&eo_time=1779760797
- **控制台 URL**: https://console.cloud.tencent.com/edgeone/pages/project/pages-itdzdcawkndt/deployment/s23vf8trec
- **项目 ID**: pages-itdzdcawkndt
- **构建时间**: 3.09s（npm install 2.2s + vite build 1.56s）
- **Edge Functions**: 编译成功，已包含 KV 修复版 admin/stats

## 密码修改功能开发 (10:15)

### 新增功能
- **修改密码 API**: `POST /api/auth/change-password` — JWT 鉴权 + 密码验证 + SHA-256 哈希存储
- **密码可见性切换**: 当前密码、新密码、确认密码三个字段均增加 Eye/EyeOff 图标切换
- **Mock 支持**: Mock 模式下验证已知密码并返回成功

### 修改文件 (5 files, +165/-27)
| 文件 | 变更 |
|------|------|
| `edge-functions/api/[[default]].js` | 新增 `handleChangePassword` 函数 + 路由注册 |
| `src/lib/api.ts` | ApiClient 新增 `changePassword()` + Mock handler |
| `src/services/authService.ts` | 新增 `changePassword()` 方法 |
| `src/pages/SettingsPage.tsx` | 修复 `savePassword` 调用 API + 密码可见性切换 + Eye/EyeOff 图标 |

### 部署
- **GitHub push**: a93f769 → feat: add change-password API + visibility toggle
- **EdgeOne**: 项目提供商已切换为 GitHub，推送后自动部署

## Mock 模式密码修改不生效修复 (10:32)

- **问题**: 修改密码后登录界面仍需用旧密码登录，新密码无效
- **根因**: Mock 模式下 `changePassword` handler 只验证旧密码返回 success，但从未保存新密码；`login` handler 始终使用硬编码密码（`admin→123456`，`joan→11223344`）
- **修复方案**: 新增 localStorage 模拟密码存储（`mock_pwd:{username}` key），修改密码时写入、登录时读取
- **修改文件**: `src/lib/api.ts`
  - 新增 `getMockPassword(username)` / `setMockPassword(username, pw)` helper 函数
  - `changePassword` mock: 从 localStorage 读取用户名 + 当前密码，验证后保存新密码
  - `login` mock: 从 localStorage 读取密码匹配，替代硬编码；非 admin/joan 用户也需密码匹配
- **验证**: TypeScript 零错误，Vite 构建成功 1.39s
- **GitHub push**: a8579d7 → fix: Mock模式密码修改不生效

## 密码修改Bug根因修复 — 第二弹 (10:55)

- **问题复现**: 用户报告修复后问题依旧，修改密码后仍需旧密码登录
- **根因发现**: `.env` 文件中 `VITE_MOCK_MODE=false` 导致应用运行在**真实API模式**
  - 之前 commit a8579d7 的修复只修改了 Mock 处理器 (`src/lib/api.ts`)
  - 但 `.env` 强制 `IS_MOCK=false`，Mock handler 从未被调用！
  - 所有请求发往 Edge Function 后端，Mock 修复形同虚设
- **额外修复**: `register` Mock handler 未调用 `setMockPassword` 保存注册密码
- **修改文件**:
  1. `src/lib/api.ts`: register handler 新增 `setMockPassword(username, password)` 调用
  2. `.env`: 移除 `VITE_MOCK_MODE=false`，恢复默认 Mock 模式（`IS_MOCK=true`）
  3. 生产部署由 CI/CD 环境变量控制真实 API 模式
- **验证**: TypeScript 零错误，Vite build 1.44s 成功
- **GitHub push**: 1abec32 → fix: 密码修改Bug根因修复 — 恢复Mock模式 + register持久化密码

## 真实后端模式全面优化 + 新项目部署 (11:17-11:45)

### Phase 1: 环境配置 + 客户端修复
- **`.env`**: 设置 `VITE_MOCK_MODE=false` + `VITE_API_URL=/api`
- **`src/services/api.ts`**: IS_MOCK 逻辑修正为 `VITE_MOCK_MODE !== 'false'`（与 `src/lib/api.ts` 统一）

### Phase 2: pages.config.json 路由修复
- catch-all dest 从 `/api/index.js` 修正为 `/api/[[default]].js`

### Phase 3: Edge Function 路由补全 (核心文件)
- **注册路由**: `handleRegister` 接入路由分发（之前函数存在但未注册）
- **用户设置**: 新增 `GET/PUT /api/settings`（DEFAULT_SETTINGS 常量 + KV 持久化）
- **论文导出**: `GET /api/papers/export?format=json|bibtex|csv`
- **Notes/Highlights**: 从 mock 空数组改为 KV 真实读写
- **文库移除论文**: `DELETE /api/libraries/{libId}/papers/{paperId}`
- **语法修复**: 编辑残留代码通过 `node --check` 修复

### 构建验证
- ✅ `node --check` JS 语法通过
- ✅ `npx tsc --noEmit` TypeScript 零错误
- ✅ `npm run build` Vite 生产构建 1.39s
- ✅ Mock 代码树摇消除（`handleMockRequest` 从产物中消失）

### GitHub 推送
- **Commit**: f08e31c — feat: switch to real EdgeOne Pages backend mode
- **Push**: 远程确认 HEAD 已指向 f08e31c

### 新 EdgeOne Pages 部署 (academic-hub-v6)
- **项目名**: academic-hub-v6（新建项目）
- **项目 ID**: pages-gzlkssgpq3iu
- **部署 ID**: cwvq1pq405 (首次), dwa83z2nuh (第二次含环境变量)
- **环境变量**: `VITE_MOCK_MODE=false`, `VITE_API_URL=/api`
- **部署 URL**: https://academic-hub-v6-3sutpo9l.edgeone.cool
- **控制台**: https://console.cloud.tencent.com/edgeone/pages/project/pages-gzlkssgpq3iu/deployment/dwa83z2nuh
- **CLI 版本**: 1.4.5
- **构建时间**: 2.2s (npm install 1.9s + vite build 2.2s)
- **Edge Functions**: 已编译 (137KB) + 部署

### 验证结果
- ⚠️ 预览链接保护 (eo_token) 导致 POST API 测试受限（302 重定向 → Method Not Allowed）
- ✅ 前端页面在浏览器中可正常加载（preview_url 已验证）
- ✅ Edge Function 文件结构正确（`.edgeone/edge-functions/index.js` 137KB）
- ✅ 环境变量已配置并注入构建（构建日志显示 `[cli][✔] Pulling environment variables to .env succeed!`）
- ⚠️ POST API 外部验证需禁用预览保护或绑定自定义域名

## AI 聊天 URL 重复拼接 Bug 修复 (17:23)
- **Bug**: `callOpenAICompatibleApi` 无条件追加 `/chat/completions` 到 `baseUrl`
- **根因**: 用户配置的 `baseUrl` 已包含 `/chat/completions`（如豆包/Doubao API `.../api/v3/chat/completions`），追加后产生 `.../chat/completions/chat/completions` → 404
- **修复**: 先正则剥离已有 `/chat/completions` 后缀，再统一追加
- **Commit**: f968d94 → push → edgeone deploy (deployment: 4nskhqdkl6)
- **环境变量清单**: VITE_MOCK_MODE=false（✅）, VITE_API_URL=/api（✅）, JWT_SECRET（⚠️ 待配置，当前回退硬编码值）, KV绑定 ACADEMIC_HUB_KV（⚠️ 待确认控制台绑定状态）

## AI 对话持久化实现 (17:35-17:45)
- **Backend (Edge Function)**:
  - 新增 AI 对话 KV CRUD 辅助函数：`getConvIndex`、`updateConvIndex`、`removeFromConvIndex`、`getConversation`、`saveConversation`
  - KV key 模式: `ai_conv_index:{userId}` (对话列表索引) + `ai_conv:{userId}:{convId}` (对话内容)
  - `GET /api/ai/conversations` 改为真实 KV 读取（返回完整对话含 messages，按 updatedAt 降序）
  - 新增 `PUT /api/ai/conversations/:id` 保存/更新对话
  - 新增 `DELETE /api/ai/conversations/:id` 删除对话
  - `handleAiChat` 支持 `conversationId` 历史上下文（从 KV 取最近 20 条消息拼接到 AI 请求中）
  - 所有对话路由需 JWT 认证
- **Frontend (AIChatPage.tsx)**:
  - `api.ts` 新增 `getConversation`、`saveConversation`、`deleteConversationRemote` 方法
  - SSE 流式完成后自动同步 conversations 状态 + 持久化 KV
  - JSON 模式完成后自动持久化 KV
  - `deleteConversation` 同步调用 API 删除 KV
  - 使用 `useRef(messagesRef/conversationsRef)` + `setTimeout(200ms)` 确保异步状态写入可靠
  - 修复 SSE 模式下 conversations 状态不同步的 bug
- **Commit**: 1eb6c73 → push → deploy (deployment: 56y71y10aw)
- **效果**: 刷新页面/重新打开后对话完整保留；删除后从 KV 彻底清除；AI 感知完整对话历史
