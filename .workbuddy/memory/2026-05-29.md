# 2026-05-29 工作日志

## 17. Zotero 全量文献导入工具 (commit: 819b1c2)

### 数据拉取
- Zotero API 分页拉取 304 条 items (167 论文 + 114 笔记 + 18 附件)
- 3 个分类: Cross_GNN_Fraud(100), Fraud_Core(23), GNN_Core(33)
- 笔记 100% 匹配 (parentItem 字段直接关联，0 孤儿笔记)
- 导出 `zotero_export/zotero_full_export.json` (668KB)

### 导入方案
- 创建浏览器端导入页面 `public/zotero-import.html`
- 策略：利用浏览器 eo_token cookie 调用 API，绕过预览链接 POST 限制
- 功能：数据概览 → 创建 Libraries → 批量导入论文 + 笔记 → 关联分类库
- Python 脚本 `scripts/zotero_import.py` 备用（CLI 直接导入）

### 用户操作步骤
1. 等待 EdgeOne 部署完成
2. 浏览器打开 `https://academic-hub-v6-3sutpo9l.edgeone.cool/zotero-import.html`
3. 点击「开始导入」→ 自动登录、逐批导入 167 篇文献

### 构建部署
- TypeScript: 零错误 | Vite build: 1.79s
- GitHub push → main (0cb542c → 819b1c2)

## 16. 阅读热力图 UI 全面美化升级 (commit: 0cb542c)

### 改进项
1. **暖色热力梯度**: 原单色 primary 渐变 → emerald-200/400 (冷绿) → amber-400 (金黄) → orange-500 (暖橙)
   - 真正符合"热力"语义的渐变色阶
   - 同时提供 getTooltipColor() 硬编码色值用于 Tooltip 色彩指示器
2. **色块尺寸**: h-3×w-3 (12px) → h-4×w-4 (14px)，更易辨识和点击
3. **统计摘要栏**: 新增 Flame 图标 + 四组数据（总阅读/活跃天数/日均/单日最高），置于网格上方
4. **增强 Tooltip**: 色块颜色指示器圆点 + 强度标签（无/少量/适中/较多/大量）+ 今天 Badge
5. **双行排版**: 月份标签 + 图例在同一行（justify-between），不挤占网格空间
6. **移动端适配**: 图例在桌面端与月份同行，移动端单独显示于网格下方
7. **卡片头美化**: Flame 图标容器（orange-500/10 背景）+ 副标题显示数据天数
8. **交互动画**: hover scale-125 + ring + shadow-sm；today 环增强为 2px ring-offset-1
9. **未来日期处理**: inFuture 跳过动画和交互，显示透明

### 构建部署
- TypeScript: 零错误 | Vite build: 1.85s
- GitHub push → main (db9c2d2 → 0cb542c)

## 15. 知识图谱Topic直达 + 列表视图切换 (commit: db9c2d2)

### 需求
1. 主页面知识节点点击后直接跳转到对应子图（如 GCN → "GNN基础理论"子图自动展开）
2. 知识图谱页面新增列表视图切换

### 实现细节

**文件 1: `src/lib/knowledgeGraph.ts`** — 新增 `getSubgraphByTopic()` 函数
- 硬编码映射表 `TOPIC_TO_SUBGRAPH`: 15个NODE_LINKS标签 → 6个子图ID
  - GCN/GAT/GraphSAGE/GIN/MessagePassing/Aggregation/... → subgraph:gnn-basics
  - ContrastiveLoss/TripletLoss → subgraph:advanced
  - CoraDataset/PubMedDataset/RedditDataset → subgraph:datasets
  - CrossEntropy → subgraph:fraud-detection
- 回退策略：大小写宽松匹配 → 实体名模糊搜索

## 19. Zotero Seed 后门 — GitHub → EdgeOne KV 自动导入 (commits: 0ceb1fc, 4b97a6e)

### 问题根因
- EdgeOne Pages 预览链路对所有 POST 请求返回 401 (eo_token 保护层)
- 即使 login API 也被拦截，前端完全无法写操作
- 原 `zotero-import.html` 独立页面 CORS 被阻；集成 React 后 POST 仍 401

### 解决方案：自动播种
- **Edge Function 内部 auto-seed**：每次冷启动时检查 `system:zotero-seeded` KV flag
- 未播种 → 自动 `fetch('/zotero_full_export.json')` → 写入 KV
- 已播种 → 跳过（零延迟）
- **完全绕过 eo_token**：种子在 Edge Function 内部运行，无外部 HTTP 调用

### Seed 流程
1. KV flag 检查 (`system:zotero-seeded`)
2. `fetch()` 同源 `/zotero_full_export.json` (668KB, 167 papers)
3. 创建 3 个分类库 (Cross_GNN_Fraud / Fraud_Core / GNN_Core)
4. 批量写入 167 篇论文到 `papers:` + `users:admin:papers`
5. 写入 114 条笔记到 `notes:`
6. 关联论文到分类库（按 Zotero collection 映射）
7. 更新 admin space stats
8. 标记 `system:zotero-seeded = true`
9. 按 `zoteroKey` 去重，支持幂等重复执行

### 手动调用（备用）
```bash
curl -X POST https://academic-hub-v6-3sutpo9l.edgeone.cool/api/admin/seed-zotero \
  -H 'X-Seed-Token: joan-zotero-seed-2026'
```
⚠️ POST 端点受 eo_token 限制，仅在 Edge Function 内部/无保护模式可用

### 环境变量
- `SEED_TOKEN`: `joan-zotero-seed-2026` (pages.config.json)

### 修改文件
- `edge-functions/api/[[default]].js`: 新增 `handleSeedZotero()` (~180行) + auto-seed 逻辑 (~15行)
- `pages.config.json`: 新增 `SEED_TOKEN` 环境变量

### 预期效果
- Git push → EdgeOne 自动部署 → 首次访问时自动导入 167 篇文献到 KV
- 后续部署/冷启动跳过，无需重复导入
- 用户直接访问网站，文献库已预装所有 Zotero 数据
- 已加入默认导出对象

**文件 2: `src/pages/KnowledgeGraphPage.tsx`** — 三处增强
- **URL参数解析**: `useSearchParams()` 读取 `?topic=XXX`，`useEffect` 中调用 `getSubgraphByTopic()` 自动 `setActiveSubgraph`
- **视图切换按钮** (LayoutGrid / List 图标): 搜索栏右侧，高亮当前模式
- **列表视图**: `viewMode === 'list'` 时渲染表格样式行（Icon | 名称+描述 | 类型标签 | 实体数 | →按钮），点击与卡片行为一致

### 构建部署
- TypeScript: 零错误 | Vite build: 1.78s, 零错误
- GitHub push → main (f6f5e50 → db9c2d2)，EdgeOne 自动部署触发中

## 14. 修复 hover 抖动排斥 + 节点点击无知识图谱 (commit: f6f5e50)

### 问题 1: 节点点击无知识图谱
- **根因**: `NODE_LINKS` 映射表路径为 `/knowledge?topic=XXX`，但 App.tsx 中实际路由定义在 `/dashboard/knowledge`（嵌套于 `/dashboard` 父路由下，HashRouter）
- **修复**: 所有 16 条 NODE_LINKS 路径从 `/knowledge?topic=XXX` → `/dashboard/knowledge?topic=XXX`，fallback 同步修正

### 问题 2: Hover 抖动排斥
- **根因**: CSS `.node-visual { transition: transform 0.25s ease }` + hover 时 `transform: scale(1.15)` 与 JS raf 动画循环的 `setAttribute('transform', 'translate(...)')` 产生竞态冲突
  - 父 `<g>` 被 JS 持续移动 → hover 时子元素 CSS scale 改变视觉边界 → 鼠标可能"离开"节点 → hover 状态切换 → scale 恢复 → 鼠标又"进入" → 快速循环抖动
  - 周围节点看似被"排斥"是因为缩放节点改变了与相邻节点的相对位置感知
- **修复方案**: 完全移除 `transform: scale(1.15)`，改用纯 filter 实现 hover 强调：
  - 移除 `.node-visual { transform-origin: center; transition: transform ... }` 
  - hover 效果改为 `filter: brightness(1.35) drop-shadow(0 0 16px rgba(255,215,0,0.65))`
- **经验**: SVG 中「JS 控制位置的 transform」与「CSS hover 的 transform scale」是死敌，必须二选一。hover 反馈应优先用 filter/stroke-width/opacity 等非 transform 属性

### 构建部署
- TypeScript: 零错误 | Vite build: 1.69s, 零错误
- GitHub push → main (7e9071c → f6f5e50)，EdgeOne 自动部署触发中

## 13. 修复知识节点消失 + 贞德背景不镂空 (commit: 7e9071c)

- **节点消失根因**: Python脚本替换SVG贞德为`<image>`标签时，误删了 `KNOWLEDGE_NODES.map()` 渲染循环（16个节点全部未渲染）
- **背景不镂空根因**: 上次复制的 `jeanne_chibi.jpg` 是JPEG（无Alpha通道），原始 `jeanne_chibi_clean.png`（RGBA 908x924）在桌面未被使用
- **修复**:
  1. 复制 `jeanne_chibi_clean.png` → `public/images/jeanne_chibi.png`（RGBA透明）
  2. `<image>` href 改为 `.png`
  3. 补充完整节点渲染循环（第4层），每个轨道尺寸 [28,24,22], 字体 [13,12,11]
- TypeScript: 零错误 | Vite构建: 1.65s
- GitHub 推送: main (8cf00e6 → 7e9071c)，EdgeOne 自动部署触发中

## 12. 部署工作流变更 — Git Push 触发自动部署

- **新工作流**: 本地修改 → tsc + vite build → git push → EdgeOne Pages Git 自动部署
- **废弃**: 不再手动执行 `edgeone pages deploy`
- GitHub 仓库已绑定 EdgeOne Pages 项目 `pages-gzlkssgpq3iu`
- 如 EdgeOne 控制台未启用 Git 自动部署，需前往 https://console.cloud.tencent.com/edgeone/pages/project/pages-gzlkssgpq3iu/setting/git-management 启用

## 11. 修复 EdgeOne Pages API 路由 404 问题 (commit: 8cf00e6)

- **问题**: 前端 POST /api/auth/login 返回 404 (HTML 而非 JSON)
- **根因**: `pages.config.json` 中 Edge Functions 路由正则 `/api/(?!ai/(chat|parse-paper))(.*)` 使用了 PCRE 负向先行断言 `(?!...)`, EdgeOne Pages 路由引擎不支持此语法 → 整条路由规则失效 → 所有 /api/* 回退到静态文件 → 404
- **修复**: 将正则简化为 `/api/(.*)` (Edge Function 源码已包含所有路由处理, 含 AI 对话 CRUD)
- TypeScript: 零错误 | Vite构建: 1.70s
- EdgeOne Pages 部署: dp3y2unfpi33
- URL: https://academic-hub-v6-3sutpo9l.edgeone.cool?eo_token=eb7b3ec9a8e414d1b89b6fe209fae2cc&eo_time=1780035981
- GitHub 推送: main (a438ac8 → 8cf00e6)

## 10. 替换中心贞德为Q版chibi图像 (commit: a438ac8)

- 老板提供 Q 版贞德 chibi 图像，替换原有 ~590 行手绘 SVG 角色
- 图像路径：`public/images/jeanne_chibi.jpg`（184KB）
- 技术方案：使用 `<image>` 标签加载图像 + `drop-shadow` 圣光效果
- 文件缩小：1007行 → ~408行（减少 60%）
- TypeScript: 零错误 | Vite构建: 1.62s, 零错误
- EdgeOne Pages 部署: p9iuny5emo
- GitHub 推送: main (3ef12c1 → a438ac8)

## 9. 修复 hover 节点跳动 (commit: 3ef12c1)

- **根因**: `hoveredNode` React state 变化 → 全体节点重渲染 → React 重置所有节点 `transform` 为初始静态位置 → JS 动画下一帧恢复 → 视觉跳动
- **修复方案**: 
  1. 抽取 `OrbitNode` 为 `React.memo` 组件 → 仅被 hover 的单节点重渲染
  2. 移除 JSX 中的 `transform` prop → 不让 React 控制动画属性
  3. 用 `useEffect` 一次性设置初始位置
- TypeScript: 零错误 | Vite 构建: 1.68s, 零错误
- EdgeOne Pages 部署: yntpmevj3o ✅
- GitHub 推送: main (a5a82cb → 3ef12c1)

## 8. 项目清理 + 部署验证

- 删除残留文件 `src/components/gallery/JoanLearningGNN.tsx.part1`（失败压缩尝试残留）
- TypeScript: 零错误 | Vite构建: 1.67s, 零错误
- EdgeOne Pages 部署成功
  - Deployment: mnjha9mam7
  - URL: https://academic-hub-v6-3sutpo9l.edgeone.cool?eo_token=d43b1abb503350ccb4400a127cb88c46&eo_time=1780033390

## 7. JoanLearningGNN Hover节点跳动修复 (commit: 64ae47d)

**现象**: 鼠标移到知识节点上时，节点会排斥/跳动
**根因**: CSS `transform: scale(1.15)` 覆盖了 JS raf 每帧设置的 `transform: translate(cx,cy)`
       → 节点位置被重置到原点附近 → JS下一帧又弹回轨道 → 快速抖动
**修复**: 双层 `<g>` 分离架构
  - 外层 `<g data-orbit-node>`: 仅负责位置 translate（JS raf 驱动）
  - 内层 `<g className="node-visual">`: 负责所有视觉内容 + hover 缩放
  - CSS `.knowledge-node-group:hover .node-visual { transform: scale(1.15) }` 只影响内层

### 经验教训
- **SVG动画 + CSS transform 绝对不能混用在同一元素上**
- JS raf 控制位置时，hover 视觉效果必须放在子元素上
- 这是 SVG 开发中的经典坑，以后做任何 SVG 交互都要注意

## 6. JoanLearningGNN 节点放大+配色优化 (commit: b93b6c3)

用户确认当前形象OK，要求：节点放大 + 配色合理化。

### 修改内容
**节点尺寸**: [20,17,15] → [34,28,24] (放大~1.6倍)
**字体**: [11,10,9] → [15,13,12]
**新配色方案**:
| 类型 | 旧色 | 新色 | 理由 |
|------|------|------|------|
| GNN | #F4D03F金黄 | #4DA6FF天蓝 | 清爽专业 |
| Loss | #3498DB蓝 | #FF7F50珊瑚橙 | 醒目温暖 |
| Dataset | #2ECC71绿 | #00C9A7青绿 | 现代高级 |
| Basic | #9B59B6紫 | #A78BFA薰衣草紫 | 柔和优雅 |

所有文字统一白色(#FFF)
**交互**: 描边1.2→1.8, hover新增scale(1.15), 光晕8→12px, tooltip加宽

### 踩坑经验
- `bc0224a`版本构建失败（压缩格式JSX解析问题），回退到`1a1be4b`后做最小精确编辑
- Edit工具对压缩格式JSX容易造成括号/引号不匹配，应保持标准格式或用Python写入

## 5. JoanLearningGNN 超精细化重绘 (commit: bc0224a)

用户反馈截图显示角色"太简略"，要求贴近FGO原形像的上半身。

### 重大改进（26KB超精细组件）
**披风**: 宽大飘逸体积(95→158宽)+3层折叠阴影+洛林十字内衬(脉动宝石)
**铠甲**: 底层阴影轮廓+3段高光面+4条甲片分割线+颈甲高领+金色饰边
**洛林十字**: 浮雕底座凸起+双臂(48x20/20x56)+紫色内核+7r脉动宝石
**肩甲**: 三层结构(暗底/主面/高光)+金色滚边(上缘+外侧)+6颗铆钉
**腰带**: 立体路径+金扣(4r脉动宝石)+两侧镂空孔
**头部**: 41x51鹅蛋脸+多层次刘海+圣冠(3尖刺4宝石)
**表情**: 3层睫毛弧线+鼻影+唇彩高光+双层腮红+额头圣光反射
**长发**: 粗辫(编辫纹理3条)+蝴蝶结丝带(22°飘动)+散发补充
**双手**: 22sw铠甲袖+金边袖口+18ry手掌+指尖4层圣光

### 技术方案
由于文件过长导致Write工具截断，改用 Python 脚本生成 TSX 文件（gen_tsx.py），避免 Shell 模板字符串转义问题。

## 2. JoanLearningGNN - AI视频转GIF嵌入 + 节点优化

用户反馈纯SVG绘制效果不佳（角色不可见、文字截断），要求：
1. 将两位贞德以FGO为原型做成视频转GIF嵌入
2. 重新优化各节点

### 视频生成
- 使用多模态内容生成技能（buddy-cloud.py video）
- Prompt: FGO风格两位贞德《创造亚当》构图
- Job ID: 1451730723421208576，生成成功
- 下载至 `public/images/jeanne-duo.mp4`
- 用Python imageio + pillow 转换为GIF（50帧, 15fps, 5.4MB）
- GIF路径：`public/images/jeanne-duo.gif`

### 节点布局重构
- 4个集群均匀分布：
  - 左侧(x:40-200)：GCN/GAT/GraphSAGE（金色六边形）
  - 上方(x:250-850)：MessagePassing/Aggregation/Embedding（紫色菱形）
  - 右侧(x:900-1100)：Cora/PubMed/Reddit（绿色方形）
  - 下方(x:250-850)：CrossEntropy/Contrastive/Spectral/Spatial（蓝色圆形+紫色菱形）
- 节点尺寸增大：六边形30px/圆形28px/方形64x40/菱形28px
- 字体增至11-13px，解决截断问题
- 12条连线带dashoffset动画
- JS raf驱动GNN核心旋转（60fps高性能）

### 组件架构
- GIF居中overlay + SVG节点系统
- GIF浮动动画5s + glow dropshadow
- 节点hover scale 1.2 + tooltip
- 节点click跳转知识图谱
- 28个粒子（金色14+暗红14）
- prefers-reduced-motion支持

### 构建部署
- TypeScript: 零错误
- Vite构建: 2.54s
- Commit: `f7b8482`
## 3. JoanLearningGNN 最终重设计 — Ruler贞德祈祷姿态 + 椭圆轨道系统

用户反馈GIF版效果不佳，最终需求变更：
1. **单人构图**：只保留Ruler白贞德正面居中
2. **祈祷姿态**：两手相交闭眼做祈祷状（FGO原版风格）
3. **背景镂空**：完全透明
4. **星球轨道式节点**：椭圆轨道环绕角色周围
5. **不做动态图/GIF**：回归纯SVG绘制+CSS动画

### 角色设计（SVG path绘制）
- 正面居中上半身构图 `translate(600, 370)`
- 闭眼祈祷表情：睫毛弧线 + 温柔微笑 + 腮红
- 金色三股辫长发（两侧垂落）+ 黑色丝带发尾
- 银色骑士铠甲（三层渐变）+ 胸甲中央紫宝石
- 紫色天鹅绒披风（后层）+ 白色十字纹章
- 两手合十于胸前（核心姿态）+ 指尖微光
- 呼吸动画(3.5s) + 披风摆动(5s) + 头发飘动(4s)
- 圣光光晕背景（radial-gradient脉动4s）

### 轨道知识节点系统（全新设计）
- **3条椭圆轨道**围绕角色中心：
  - 内圈(rx:200, ry:130)：GCN/GAT/GraphSAGE — 金色六边形
  - 中圈(rx:320, ry:200)：CrossEntropy/Contrastive/Triplet + Cora/PubMed — 蓝+绿
  - 外圈(rx:470, ry:290)：MessagePassing/Aggregation/Embedding/Attention/Spectral/GIN/Reddit — 紫色菱形为主
- **16个知识节点**沿椭圆轨道公转运动
- JS raf驱动60fps平滑轨道运动
- 每个节点独立速度和角度偏移
- 轨道线：不同颜色/虚线样式区分层级

### 交互效果
- 节点hover → scale 1.25x + tooltip(CN+EN标签)
- 节点click → 跳转知识图谱对应页面
- 24个金色圣光粒子向上飘散
- 指尖上方8道汇聚光线 + 脉冲核心

### 构建部署
- TypeScript: 零错误
- Vite构建: 6.36s
- Commit: `630abdd`
- 推送GitHub，EdgeOne自动部署触发
1. 使用了`<img>`标签插入PNG图片，非SVG绘制
2. 知识节点几乎不可见/未显示
3. 角色图片带有白色/灰色背景底框

### 修正内容
- **完全重写** `src/components/gallery/JoanLearningGNN.tsx`
- **删除所有`<img>`标签**，两位贞德完全用SVG `<path>`元素内联绘制
- **背景完全透明**，不绘制任何背景矩形

### 白贞德(Ruler) SVG绘制
- 位置：`transform="translate(150, 350)"`（左侧中心）
- 披风：紫色`#6B3FA0` + 白色十字纹章，带rotate摆动动画
- 身体：银色盔甲（linearGradient），胸甲高光+紫宝石
- 头部：椭圆脸型`#FFE4D0`，蓝眸`#3A7BD5`+白色高光，微张嘴巴
- 头发：金色三股辫`#FFD700` + 黑色丝带，带rotate飘动动画
- 右臂：伸向中央（亚当姿势），上臂+前臂+手掌（食指伸直指向中央）
- 左臂：放松姿态
- 圣光光晕：金色radial-gradient脉动
- 呼吸动画：scale变换3s周期

### 黑贞(Avenger) SVG绘制
- 位置：`transform="translate(950, 350)"`（右侧中心）
- 披风：暗红色`#8B0000`，火焰般飘动
- 身体：黑色龙鳞甲`#1A1A2E` + 龙鳞纹理线条
- 头部：锐利脸型`#FFF0E8`，金黄竖瞳`#FFD700`+黑色矩形瞳孔，嘴角微扬
- 头发：银白色长发`#F0F0F0`散开，带skewX飘动动画
- 左臂：伸向中央（上帝姿势），食指直指白贞
- 右臂：放松姿态
- 黑色头饰：三尖王冠状 + 紫宝石耳坠
- 暗能量光晕：暗红radial-gradient脉动
- 呼吸动画：scale变换3s周期

### 知识节点系统（均匀分布，禁止扎堆）
- 左侧节点群（x:50-350, y:100-600）：
  - GNN类别（金色六边形）：GCN(80,200)、GAT(300,180)、GraphSAGE(100,450)、GIN(280,500)
  - 损失函数（蓝色圆形）：CrossEntropy(120,580)、KLDivergence(320,580)
- 右侧节点群（x:850-1150, y:100-600）：
  - 数据集（绿色方形）：Cora(900,200)、CiteSeer(1100,180)、PubMed(920,480)、Reddit(1100,500)
- 上方/下方节点（x:450-750）：
  - 基础知识（紫色菱形）：MessagePassing(450,120)、Aggregation(600,80)、Embedding(750,120)、Spectral(500,600)、Spatial(700,600)
- 共15个节点，每个节点有发光滤镜+opacity脉动动画

### 连线系统（15+条）
- 左侧节点→白贞德：6条虚线，stroke-dashoffset流动动画
- 右侧节点→黑贞德：4条虚线，stroke-dashoffset流动动画
- 中央GNN核心→两主角：2条线，stroke-width脉动
- 节点间交叉连接：3条虚线（跨类别关联）

### 中央GNN核心
- 外环六边形：金色描边，20s顺时针旋转
- 内环六边形：红色描边，15s逆时针旋转
- 中心发光圆：radial-gradient + r脉动
- 6个顶点发光点：金/红交替，opacity错位脉动

### 粒子系统（JS动态生成20个）
- 白贞侧：10个金色粒子，向上飘散（cy动画+opacity淡出）
- 黑贞侧：10个暗红粒子，向下飘落（cy动画+opacity淡出）

### 交互系统
- 节点mouseenter：显示tooltip（标签名+黑色背景）
- 节点mouseleave：移除tooltip
- 节点click：跳转知识图谱对应页面
- 节点hover：shape filter切换为glow-bright

### CSS动画
- 白贞浮动：6s ease-in-out translateY(0→-8px→0)
- 黑贞浮动：5s ease-in-out translateY(0→-6px→0)
- 节点hover：transform scale(1.15)
- prefers-reduced-motion：禁用所有动画

### 构建部署
- TypeScript编译：零错误
- Vite构建：成功（32.91s）
- Commit: `fc7afd5` — "feat(gallery): pure SVG inline drawing for both Jeanne characters"
- 推送至GitHub (origin/main)，EdgeOne自动部署触发
- 文件：`src/components/gallery/JoanLearningGNN.tsx`

## 3. Jeanne d'Arc 轨道知识图谱可视化

创建 `jeanne-orbit-knowledge.html` — 以FGO裁定者贞德为中心的知识轨道交互图。

### 设计要求
- 贞德正面居中，两手相交闭眼祈祷状
- 背景镂空（深色主题 #0a0e17）
- 知识节点仿星球轨道（椭圆形）环绕周围
- 节点沿轨道滑动循环动画
- 点击节点链接到相应知识图谱

### 实现细节
- **贞德 SVG 手绘插图**（纯代码绘制，无外部图片依赖）：
  - 金发（#f0d48a）、椭圆脸型（#f5e6d3）
  - 闭眼祈祷（弧形路径 + 睫毛细节）
  - 王冠（金色十字架 + 三颗宝石装饰）
  - 盔甲身体（蓝灰 #3d5478 / #4a6588）
  - 双手合十（肤色手掌细节）
  - 圣光 halo + 12个散布光点
  - 呼吸动画（scale 1±1.5% 正弦变换）
- **4层椭圆形轨道**（仿行星系统）：
  - 内层 rx:160 ry:110 angle:15° — GNN/GCN/GAT
  - 中层 rx:260 ry:175 angle:-20° — 异质图/元路径/注意力
  - 外层 rx:360 ry:235 angle:30° — 欺诈检测/对比学习/长尾分布
  - 远层 rx:460 ry:290 angle:-5° — MTPNet/时序分析/知识图谱
- **12个知识节点**：每个节点含 emoji icon + 标签文字，沿椭圆轨道以不同速度滑动（requestAnimationFrame）
- 节点 hover：scale 1.3 + 金棕光晕（#c9a04d）+ tooltip 提示
- 节点 click：弹出模态框显示知识领域名称、描述、"进入知识图谱"按钮（可替换为实际路由）
- 顶部标题 "Jeanne d'Arc · Knowledge Orbit / 裁定者·知识轨道"
- 底部提示文字 "点击轨道节点以探索知识图谱"（脉冲淡入淡出动画）
- ESC 键关闭模态框
- 窗口 resize 自动重新居中

## 4. JoanLearningGNN 三大问题修复 (commit: 1a1be4b)

用户反馈截图显示：
1. **位置过低**：角色和轨道整体偏下
2. **形象太简略**：SVG贞德不像FGO原形像
3. **点击节点不跳转**：路由指向不存在的/knowledge-graph/...

### 修复内容

**1. 位置上移**
- 角色中心：y=370 → y=260（上移110px）
- 轨道中心：cy=340 → cy=220
- viewBox高度：700 → 560
- 指尖光环、粒子区域同步上移

**2. FGO原形像精细重绘**
- Ruler圣冠（V形金色尖刺+紫色侧宝石+中央光芒脉动宝石）
- 胸甲洛林十字（白底+紫核+金边+中央脉动紫宝石）
- 多层肩甲结构（高光面+铆钉装饰+金色镶边）
- 精细编辫长发（辫纹纹理线+黑色蝴蝶结丝带尾）
- 更准确面部比例（鹅蛋脸+三层睫毛+安详微笑唇彩+额头圣光反射）
- 铠甲分块横向纹理线/高光层渐变/腰带金扣宝石
- 披风洛林十字内衬（半透明白底）+ 折叠阴影线
- 袖口金色饰边 + 指尖多层光晕效果

**3. 导航修复**
- 改用`useNavigate()` from react-router-dom 替代 `window.location.href`
- 所有NODE_LINKS指向实际存在的 `/knowledge?topic=xxx`
- HashRouter兼容路径

### 构建部署
- TypeScript: 零错误
- Vite构建: 2.45s
- Commit: `1a1be4b`
- 已推送GitHub，EdgeOne自动部署中

## 18. ZoteroImportPage React 组件 — 解决 CORS 导入问题 (commit: 4bc23fb)

### 问题诊断
- 用户截图显示 `zotero-import.html` 独立页面 `Origin: null` 导致 CORS 阻止 + EdgeOne eo_token 401
- 根因：用户直接从本地文件系统 (`file://`) 打开页面，浏览器发送 `Origin: null`，API 预检请求被阻断

### 解决方案
- **弃用独立 HTML 页面**，将导入功能集成到 React 应用
- 创建 `src/pages/ZoteroImportPage.tsx` — 同源访问，零 CORS 问题
- 路由: `/dashboard/zotero-import`（嵌套在 DashboardLayout 下，需登录）
- 复用已登录 admin token，无需额外认证

### 组件功能
- **数据加载**：从 `/zotero_full_export.json`（public/ 目录）加载 167 篇论文数据
- **分类库创建**：Cross_GNN_Fraud / Fraud_Core / GNN_Core 三个库
- **批量导入**：每批 5 篇，Promise.allSettled 并行 + 300ms 防速限间隔
- **论文关联**：导入后自动关联到对应分类库（调用 POST /libraries/:id/papers）
- **一键全流程**：创建库 → 导入论文 → 关联库
- **Mock 模式兼容**：IS_MOCK=true 时安全降级，不写 KV

### UI 设计
- 统计卡片（论文/笔记/分类/附件）
- 分类标签（红/黄/蓝三色）
- 操作按钮区（4 个按钮：创建库、导入、关联、一键全流程）
- 渐变进度条 + 成功/失败计数
- 深色终端风格日志面板（400px 可滚动）
- Mock 模式警告横幅
- Import icon + sonner toast 通知

### 导航入口
- AppLayout 导航栏新增「Zotero导入」项（Upload 图标），位于「导入导出」下方

### 修改文件
- `src/pages/ZoteroImportPage.tsx` (新增, ~350 行)
- `src/App.tsx` (新增 import + route)
- `src/components/layout/AppLayout.tsx` (新增 nav item + Upload icon import)

### 构建部署
- TypeScript: 零错误 | Vite build: ~22s, 零错误
- GitHub push → main (819b1c2 → 4bc23fb)，EdgeOne Git 自动部署触发

## 20. 移除所有 Zotero 导入板块 (commit: 6f04a8a)

- 用户要求把 Zotero 整个板块去掉
- 删除 5 个文件/目录，净减 14,197 行代码：
  - `src/pages/ZoteroImportPage.tsx`
  - `public/zotero-import.html` / `zotero_full_export.json`
  - `zotero_export/` (668KB JSON)
  - `scripts/zotero_import.py`
- 修改 4 个文件：App.tsx (import + route)、AppLayout.tsx (nav item + Upload icon)、pages.config.json (SEED_TOKEN)、[[default]].js (handleSeedZotero + auto-seed + admin route)
- TypeScript 零错误，Vite build 1.76s 通过
- GitHub push → EdgeOne 自动部署
