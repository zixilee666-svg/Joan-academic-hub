# 2026-06-08 工作日志

## 部署策略调整
- 用户要求：以后修复问题后，直接推送到GitHub，不要手动部署
- 清理项目：删除根目录 `edgeone.json` (CLI手动部署生成)，添加到 `.gitignore`
- 提交并推送工作日志和 `.gitignore` 更新到GitHub

## 后续工作流程
1. 修复问题 → `git commit` + `git push origin main`
2. EdgeOne Pages自动检测GitHub推送并触发部署
3. 不手动执行 `edgeone pages deploy` 命令

## 405 错误根因与修复
- 根因：`POST /api/search/import` 被 Cloud Functions `/api/search/*` 路由拦截，Cloud Function 只支持 GET → 405
- 修复：将前端调用路径从 `/search/import` 改为 `/import`，绕过 Cloud Functions，由 Edge Functions 处理
- 涉及文件：`src/lib/api.ts`（mock+real 路径）、`src/services/searchService.ts`

## pages.config.json 恢复 Edge Functions 配置
- 发现 `pages.config.json` 缺少 `edgeFunctions` 配置（被之前提交意外移除）
- 恢复 `edgeFunctions.routes` 配置，确保自动部署能正确识别 Edge Function

## 批量管理功能（LibraryPage）
- 新增批量管理模式：点击"批量管理"进入批量选择状态
- 支持：单选/多选/全选/取消全选
- 批量操作：批量删除（乐观更新+失败回滚）、批量导出 BibTeX/CSV
- Grid 和 List 视图均支持批量选择，显示复选框
- 选中卡片高亮显示

## 提交记录
- `083c9f7` — 恢复 pages.config.json Edge Functions 配置
- `2278300` — 修复 import API 路径绕过 Cloud Functions
- `ed58484` — 添加 LibraryPage 批量管理功能
