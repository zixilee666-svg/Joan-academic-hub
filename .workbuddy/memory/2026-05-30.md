# 2026-05-30 工作日志

## 01. 研究目标新增删除功能 (commit: 92cba0b)

### 需求
研究项目卡片「研究目标」区域，添加目标后无法删除。

### 修改
`src/pages/ResearchPage.tsx`：
1. 新增 `deleteObjective(projectId, objectiveId)` 函数
   - filter 过滤目标 → 重新计算 progress/status
   - 乐观更新 + 错误回滚（与 toggleObjective 一致）
2. 目标列表每行末尾追加 `<X>` 删除按钮
   - `opacity-0 group-hover:opacity-100`：鼠标悬浮才显示
   - `hover:text-destructive`：悬浮时变红

### TypeScript: 零错误 | push → EdgeOne 自动部署

## 02. 重新推送 + Zotero 数据导入 KV (commit: 0eb6a02)

### 推送
- SSH 被沙箱阻止 → 临时切换 origin 为 HTTPS → push → 恢复 SSH

### Zotero 提取 (scripts/extract_zotero.py)
- 连接本地 Zotero SQLite，提取 155篇论文、95条笔记、3个分类、18个附件
- 输出: scripts/zotero_export.json (779KB)

### 新增 API
- `POST /api/admin/workbuddy/import-zotero-kv` 端点
- 批量写入 papers/libraries/notes 到 KV，按 zoteroKey 去重
- src/lib/api.ts → importZoteroKV() 方法
- AdminPage → "从 Zotero 导入" 按钮（上传 JSON → 一键导入）

## 04. 修复论文详情页笔记显示：双来源合并 (commit: 067c8e8)

### 问题
PaperDetailPage 笔记标签显示「暂无笔记」，但 Zotero 中有对应笔记
根因: GET /papers/:id/notes 只读 `paper.notes`（嵌入式），Zotero 导入存在独立 KV key `papers:{paperId}:notes`

### 修复
- GET notes 端点合并两个来源（embedded + Zotero KV），按 id 去重
- 笔记内容已验证：UTF-8 编码正常，HTML 格式

### 使用流程
1. EdgeOne 部署完成 → 登录 admin → WorkBuddy → "从 Zotero 导入"
2. 上传 scripts/zotero_export.json
3. 访问论文详情页 → 笔记标签自动显示 Zotero 笔记

## 05. 修复 AI 文献解析 502 超时 (commit: 4f7706d)

### 问题
POST /api/ai/parse-paper 返回 502 + net_exception_timeout
根因: Edge Function (V8 isolate) 无法访问外部网络；Cloud Function 已写好但路由未配置

### 修复
1. pages.config.json: Edge Function route 排除 /api/ai/*
   - 旧: `^/api/([a-z]+)(/.*)?$`
   - 新: `^/api/(?!ai/)([a-z]+)(/.*)?$`
2. cloud-functions/api/ai/[[default]].js: 修复导出
   - `export default async function` → `export async function`（命名导出）
3. 效果: /api/ai/* 路由 → Cloud Function (Node.js v20, 120s超时)
   - parse-paper: 55s timeout + 3层正则降级提取
