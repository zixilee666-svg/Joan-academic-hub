# 2026-05-28 工作日志

## 修复 KnowledgePage 未定义错误 (00:30)

### 问题
部署后访问 `/dashboard/knowledge` 报错：`Uncaught ReferenceError: KnowledgePage is not defined`

### 根因
1. `src/App.tsx` 中使用了 `<KnowledgePage />` 但未导入该组件
2. `src/pages/KnowledgeGraphPage.tsx` 导入了 `getLocalKG` 但 `src/lib/knowledgeGraph.ts` 未 export 该函数

### 修复 (commit c76f00e)
- `src/App.tsx`：添加 `import KnowledgeGraphPage from '@/pages/KnowledgeGraphPage'`，并将两处 `KnowledgePage` 改为 `KnowledgeGraphPage`
- `src/lib/knowledgeGraph.ts`：将 `function getLocalKG()` 改为 `export function getLocalKG()`

### 状态
- ✅ TypeScript 零错误
- ✅ Vite build 成功
- ✅ GitHub 推送完成
- 🔄 EdgeOne Pages 部署中

## 修复 CardDescription 未定义错误 (00:40)

### 问题
部署后报错：`Uncaught ReferenceError: CardDescription is not defined`

### 根因
`src/pages/KnowledgeGraphPage.tsx` 使用了 `<CardDescription>` 组件但未在导入中声明。

### 修复 (commit a190e71)
- `src/pages/KnowledgeGraphPage.tsx`：将导入改为 `import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'`

### 状态
- ✅ Vite build 成功
- ✅ GitHub 推送完成
- 🔄 EdgeOne Pages 部署中

## 添加实体详情查看功能 (00:48)

### 需求
老板反馈：知识图谱页面只有静态卡片列表，无法查看详细资料和关系。

### 实现 (commit 8e4aa76)
- 重写 `src/pages/KnowledgeGraphPage.tsx`：
  - 点击实体卡片打开详情对话框
  - 详情对话框显示：
    - 实体完整信息（名称、类型、描述、详细内容、标签、元数据）
    - 知识关系列表（带关系类型、权重、跳转）
    - 相关实体快速链接（可点击跳转）
  - 关系类型中文映射（is_a→是一种, used_in→被用于, cites→引用等）
  - 支持在关系链中点击跳转浏览

### 状态
- ✅ Vite build 成功
- ✅ GitHub 推送完成
- 🔄 EdgeOne Pages 部署中

## 美化知识图谱可视化 (01:05)

### 需求
老板反馈：知识图谱可视化"好丑"，节点太小、颜色太浅、布局拥挤。

### 改进内容 (commit fa46eb4)
- `src/components/knowledge/KnowledgeGraphCanvas.tsx` 全面重写：
  - **节点增大**：半径从 22 → 38，字体从 9px → 11px
  - **高对比度颜色**：每个类型有独立的填充色+深色边框
  - **形状区分**：概念(圆)、论文(菱形)、方法(六边形)、数据集(方)、任务(三角)
  - **阴影效果**：节点带 drop shadow，hover 时阴影加深
  - **渐变背景**：画布背景使用 subtle gradient + 网格线
  - **渐变边**：边使用源节点到目标节点的颜色渐变
  - **箭头指示**：边上带方向箭头
  - **关系标签**：边上显示中文关系标签，带白色圆角背景
  - **图例面板**：左上角显示类型图例及数量统计
  - **缩放指示器**：右上角显示当前缩放比例
  - **优化布局**：斥力增大、弹簧距离增大、边界约束，减少拥挤

### 状态
- ✅ Vite build 成功
- ✅ GitHub 推送完成
- 🔄 EdgeOne Pages 部署中
